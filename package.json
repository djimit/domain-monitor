{
  "name": "domain-monitor",
  "private": true,
  "workspaces": [
    "domain-monitor-frontend",
    "domain-variant-service"
  ],
  "scripts": {
    "test": "jest",
    "lint": "npm run lint --workspace=domain-monitor-frontend",
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=domain-monitor-frontend"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4"
  }
}
